type Author {
  id: Int!
  firstName: String
  lastName: String
  posts: [Post]
}

type Post {
  id: Int!
  title: String
  votes: Int
}

type Query {
  authors: [Author]
  author(id: Int!): Author
  posts: [Post]
}

type Mutation {
  upvotePost(postId: Int!): Post
}